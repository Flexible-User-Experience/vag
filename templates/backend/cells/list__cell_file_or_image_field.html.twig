{% extends "@SonataAdmin/CRUD/base_list_field.html.twig" %}

{% block field %}
    {% spaceless %}
        {% if object.imageName %}
            {{ object | draw_image_or_file_cell }}
            {#{{ dump(object) }}#}
            {#{% if object.imageFile.mimeType == 'png' %}#}
                {#<img src="{{ vich_uploader_asset(object, 'imageFile') | imagine_filter('60x60') }}" alt="thumbnail">#}
            {#{% else %}#}
                {#is a file#}
            {#{% endif %}#}
        {% else %}
            ---
        {% endif %}
    {% endspaceless %}
{% endblock %}
